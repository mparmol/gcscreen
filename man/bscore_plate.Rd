% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bscore.R
\name{bscore_plate}
\alias{bscore_plate}
\title{B-score positional-bias correction for one plate-run}
\usage{
bscore_plate(well_pos, auc, ctrl_cols = c(1L, 12L), max_iter = 10L)
}
\arguments{
\item{well_pos}{Integer vector of well positions (1–96) for every well in
the plate-run.}

\item{auc}{Numeric vector of AUC values, same order as \code{well_pos}.}

\item{ctrl_cols}{Integer vector of column indices that contain control wells
and should \strong{not} be corrected. Default \code{c(1L, 12L)}.}

\item{max_iter}{Maximum iterations for \code{stats::medpolish}. Default 10.}
}
\value{
Numeric vector of corrected AUC values, same length and order as
the input \code{auc}. Control-well values are returned unchanged.
}
\description{
Applies median polish (B-score) to the 8×10 sub-matrix of compound wells
(columns 2–11 of a standard 96-well plate) to remove row and column
positional effects caused by evaporation gradients, optical artefacts, or
thermal heterogeneity. Control wells (columns 1 and 12) are \strong{not} modified.
}
\details{
The correction subtracts the estimated row effect and column effect from each
compound AUC value while preserving the plate-wide overall level:
\code{AUC_corrected = AUC_raw - row_effect - col_effect}.
}
\examples{
# Simulate one plate-run: 96 wells, compound cols 2-11, ctrl cols 1 & 12
set.seed(1)
n    <- 96
wpos <- 1:n
auc_raw <- rnorm(n, mean = 40, sd = 4)
# Add a synthetic row gradient
rc <- well_to_rowcol(wpos)
auc_raw <- auc_raw + rc$row_idx * 0.5
auc_corr <- bscore_plate(wpos, auc_raw)
cor(well_to_rowcol(wpos)$row_idx[rc$col_idx \%in\% 2:11],
    auc_corr[rc$col_idx \%in\% 2:11])  # should be near 0
}
