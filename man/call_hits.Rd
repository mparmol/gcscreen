% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hits.R
\name{call_hits}
\alias{call_hits}
\title{Call hits by FDR threshold and dual-method convergence}
\usage{
call_hits(
  anova_res,
  lmm_res,
  fdr_threshold = 0.05,
  auc_rel_max = 0.99,
  exclude = NULL
)
}
\arguments{
\item{anova_res}{Data frame returned by \code{\link[=run_anova_screen]{run_anova_screen()}}, with at least
columns \code{Chemical}, \code{FDR}, and \code{AUC_rel}.}

\item{lmm_res}{Data frame returned by \code{\link[=run_lmm_screen]{run_lmm_screen()}}, with at least
columns \code{Chemical} and \code{FDR}.}

\item{fdr_threshold}{Scalar FDR cutoff applied to both methods. Default
\code{0.05}.}

\item{auc_rel_max}{Upper bound for AUC_rel: only compounds with
\code{AUC_rel <= auc_rel_max} are considered hits (growth inhibitors). Set to
\code{Inf} to include compounds with increased growth. Default \code{0.99}.}

\item{exclude}{Character vector of compound names to exclude regardless of
statistics (e.g., optical artefacts). Default \code{NULL}.}
}
\value{
A data frame of confirmed hits with columns from \code{anova_res} plus
\code{FDR_lmm} (from \code{lmm_res}), sorted by ascending \code{AUC_rel}.
}
\description{
Identifies compounds that pass an FDR threshold in both ANOVA and LMM
(the dual-convergence criterion used in the primary pipeline), and
optionally filters by a maximum AUC_rel threshold (growth inhibitors only).
}
\examples{
# Small demo (not a real screen)
anova_res <- data.frame(
  Chemical = c("DrugA", "DrugB", "DrugC", "DrugD"),
  Plate    = 1L,
  n_reps   = 3L,
  AUC_rel  = c(0.1, 0.5, 0.95, 0.2),
  p_anova  = c(0.001, 0.003, 0.6, 0.002),
  FDR      = c(0.004, 0.006, 0.6, 0.005),
  stringsAsFactors = FALSE
)
lmm_res <- data.frame(
  Chemical = c("DrugA", "DrugB", "DrugC", "DrugD"),
  FDR      = c(0.003, 0.04, 0.7, 0.06),
  stringsAsFactors = FALSE
)
call_hits(anova_res, lmm_res)
}
