% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{run_screen}
\alias{run_screen}
\title{Run the complete gcscreen pipeline on a list of plate-runs}
\usage{
run_screen(
  platerun_list,
  time_max = 60,
  ctrl_cols = c(1L, 12L),
  ctrl_label = "CONTROL",
  plates_exclude = NULL,
  cv_warn = 20,
  fdr_threshold = 0.05,
  auc_rel_max = 0.99,
  exclude = NULL
)
}
\arguments{
\item{platerun_list}{A named list of data frames, one per plate-run. Each
data frame must have:
\describe{
\item{\code{time}}{Numeric time column (hours).}
\item{\code{well_pos}}{Integer well position (1–96).}
\item{\code{od}}{Numeric OD600 measurement.}
\item{\code{Chemical}}{Character compound name or \code{"CONTROL"}.}
\item{\code{plate}}{Integer plate identifier.}
}
Names of the list are used as \code{run} identifiers.}

\item{time_max}{Maximum time (hours) to include. Measurements beyond this
are discarded. Default \code{60}.}

\item{ctrl_cols}{Integer vector of column positions (1–12) containing
control wells. Default \code{c(1L, 12L)}.}

\item{ctrl_label}{Character label for control wells in the \code{Chemical}
column. Default \code{"CONTROL"}.}

\item{plates_exclude}{Integer vector of plate IDs to exclude (e.g., those
with CV > 45\%). Default \code{NULL}.}

\item{cv_warn}{CV threshold (\%) for flagging plate-runs. Default \code{20}.}

\item{fdr_threshold}{FDR cutoff for both ANOVA and LMM. Default \code{0.05}.}

\item{auc_rel_max}{AUC_rel upper bound for calling a hit (growth
inhibitors). Default \code{0.99}.}

\item{exclude}{Character vector of compound names to exclude from hits
regardless of statistics. Default \code{NULL}.}
}
\value{
An invisible list with elements:
\describe{
\item{\code{screen_data}}{Tidy long-format data frame with one row per
well × plate-run, including AUC, AUC_relative, and B-score
corrected values.}
\item{\code{qc}}{Data frame of QC metrics per plate-run (CV, Z', SSMD,
RZ').}
\item{\code{anova_results}}{Per-compound ANOVA results with FDR.}
\item{\code{lmm_results}}{Per-compound LMM results with FDR.}
\item{\code{hits}}{Data frame of confirmed hits (ANOVA ∩ LMM, FDR <
\code{fdr_threshold}).}
}
}
\description{
High-level wrapper that applies the full pipeline to a named list of
plate-run data frames: (1) baseline correction, (2) AUC calculation,
(3) B-score positional correction, (4) control normalization, (5) QC
metrics, (6) ANOVA per compound, (7) LMM per compound, (8) FDR-based
dual-convergence hit calling.
}
\examples{
# Use the built-in example dataset
data(example_screen)
res <- run_screen(example_screen)
res$hits
}
