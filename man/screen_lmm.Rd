% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{screen_lmm}
\alias{screen_lmm}
\title{Per-compound linear mixed model with plate as random effect}
\usage{
screen_lmm(
  data,
  auc_col = "auc",
  group_col = "Chemical",
  plate_col = "plate",
  compound_name = ""
)
}
\arguments{
\item{data}{A data frame containing at least the columns specified by
\code{auc_col}, \code{group_col}, and \code{plate_col}. Must include both the compound
and control rows (labeled in \code{group_col}).}

\item{auc_col}{Name of the column containing AUC values. Default \code{"auc"}.}

\item{group_col}{Name of the column identifying compound vs. control.
Default \code{"Chemical"}. The control level must be \code{"CONTROL"}.}

\item{plate_col}{Name of the column containing the plate identifier.
Default \code{"plate"}.}

\item{compound_name}{Character string for error messages. Default \code{""}.}
}
\value{
A named list with elements:
\item{p_value}{Raw p-value for the compound effect.}
\item{estimate}{Estimated AUC difference (compound – control).}
\item{se}{Standard error of the estimate.}
\item{method}{\code{"LMM"} or \code{"ANOVA"} depending on which model was used.}
}
\description{
Fits \code{auc ~ Chemical + (1 | Plate)} using \code{\link[lme4:lmer]{lme4::lmer()}} for compounds that
appear on two or more plates. Falls back to a standard ANOVA (equivalent to
\code{\link[=screen_anova]{screen_anova()}}) when fewer than two plates are available.
}
\examples{
# Build a minimal long-format data frame
set.seed(42)
df <- data.frame(
  auc      = c(rnorm(3, 20, 2), rnorm(48, 42, 3)),
  Chemical = c(rep("DrugA", 3), rep("CONTROL", 48)),
  plate    = c(c(2, 3, 5), sample(2:6, 48, replace = TRUE))
)
screen_lmm(df, compound_name = "DrugA")
}
