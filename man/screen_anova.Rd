% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{screen_anova}
\alias{screen_anova}
\title{Per-compound ANOVA against plate controls}
\usage{
screen_anova(auc_compound, auc_control, compound_name = "")
}
\arguments{
\item{auc_compound}{Numeric vector of compound AUC values (one per
biological replicate, typically 2–4).}

\item{auc_control}{Numeric vector of control AUC values from the same
plate(s) (typically 16 per plate-run × n_reps).}

\item{compound_name}{Character string used in error messages. Default \code{""}.}
}
\value{
Scalar p-value from the F-test, or \code{NA_real_} if the model could
not be fitted (fewer than 2 observations, zero variance, etc.).
}
\description{
Runs a one-way ANOVA comparing the replicate AUC values of one compound
against the negative-control wells of the same plate(s). Returns the raw
p-value.
}
\examples{
ctrl <- rnorm(48, mean = 42, sd = 3)
cmpd <- rnorm(3, mean = 20, sd = 2)
screen_anova(cmpd, ctrl)
}
